[package]
name = "speakeasy-domain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# The only dependency this otherwise dependency-free crate takes. It exists
# for `worker_protocol`'s framed-JSON wire format, which every supervised
# local worker process has to speak — see that module's doc comment for why the
# protocol itself lives here rather than in an engine crate.
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
